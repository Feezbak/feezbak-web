import { StoryDetailsContent } from "@/components";
import { Layout } from "@/shared";

const StoryDetails = () => {
  return (
    <Layout pageTitle="Story Details">
      <StoryDetailsContent />
    </Layout>
  );
};

export default StoryDetails;
