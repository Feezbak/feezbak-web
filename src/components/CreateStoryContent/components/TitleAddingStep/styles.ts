import styled from "styled-components";
import { Col } from "antd";

export const CreationWrapper = styled(Col)`
  padding: 4rem 0;
`;

export const PreviewWrapper = styled(Col)`
  padding: 4rem 0;
`;
