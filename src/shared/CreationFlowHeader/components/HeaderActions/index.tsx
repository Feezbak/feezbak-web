import React from "react";
import { ActionsList } from "../../";
import { HeaderActionsWrapper } from "./styles";

interface Props {
  actions: ActionsList;
}

const HeaderActions = ({ actions }: Props) => {
  return <HeaderActionsWrapper>actionsfff</HeaderActionsWrapper>;
};

export default HeaderActions;
